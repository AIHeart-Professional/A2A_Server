{
  tools: {
    check_character_limit: {
      fields: [server_id, user_id]
    },
    check_for_active_character: {
      fields: [server_id, user_id]
    },
    get_my_character: {
      fields: [user_id, server_id]
    },
    get_another_players_character: {
      fields: [character_name, server_id]
    },
    create_character: {
      fields: [
        first_name, last_name, characters_name, characters_class, cursor_color, height,
        physique, age, birthday, bio, level, experience, experience_to_next_level,
        equipment, consumables, misc, cratable, key_items, currency,
        head, chest, legs, arms, accessory1, accessory2,
        left_hand, right_hand, guild, party, hp, str, def, spe,
        dex, cha, points_to_distribute, status_ailment, battle_status,
        damage, defense, current_hp, user_id, server_id, active
      ],
      rquired: [character_name, character_class, user_id, server_id]
    },
    missing_fields: {
      fields: [server_id, user_id, missing_fields]
    }
  }
}
